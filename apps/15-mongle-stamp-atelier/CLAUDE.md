@docs/skills/apps-in-toss.md
@docs/skills/tds-mobile.md

- 이 앱은 `apps/15-mongle-stamp-atelier` 범위만 수정한다.
- 한 화면 안에서 끝내고, 내부 페이지 이동은 만들지 않는다.
- 광고 칸은 항상 보여야 하고, 반복 탭이 가능해야 한다.
- 광고 SDK가 실제 연결되기 전에는 `광고 보고` 같은 확정형 카피를 쓰지 않는다.
- 현금/포인트/캐시/당첨/보장 표현은 금지한다.
- `ait deploy`와 `npm audit fix --force`는 실행하지 않는다.
