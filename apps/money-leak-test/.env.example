# Apps in Toss 1호 앱 광고 설정 예시
# 실제 값은 .env.local 또는 빌드 환경 변수에 넣고 커밋하지 않는다.

# 콘솔에서 만든 리워드 광고 그룹 ID
VITE_TOSS_REWARDED_AD_GROUP_ID=ait-ad-test-rewarded-id

# npx ait deploy에 쓰는 배포 API 키는 이 파일에 넣지 않는다.
# 사용 예: npx ait deploy --api-key "$AIT_DEPLOY_API_KEY"
